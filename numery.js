function numery(ciag){

//if (ciag.length>0){
    
    var tab=[...ciag];
    var tabNum=[];
    while (i<(tab.length-1)){
        isNan.ciag[i]=true;
        tabNum.push(tab[i]);
    }

}

numery("125dsa4812");
