function numery(ciag){

//if (ciag.length>0){
    
    var tab=[...ciag];
    var tabNum=[];
    while (i<(tab.length-1)){
        isNan.tab[i]=true;
        tabNum.push(tab[i]);
    }
console.log(split(tab));

}

numery("125dsa4812");
